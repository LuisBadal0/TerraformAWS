###Terraform controls everything wth .tfstate(don't change this file, don't send this file to repository)

###Inicialize project
#terraform init

###Check if everything is ok before deploy
#terraform plan

###Deploy 
#terraforn apply

###Console terraform for various indexes
#terraform console

###Apply change only to user name change
#terraform apply -target="aws_iam_user.my_iam_user"

###Validade context
#terraform Validade

###Format document
#terraform fmt

###Shows states in tfstate
#terraform show

###Plan terraform command to a file
#terraform plan -o iam.tfplan

###Apply plan
#terraform apply iam.tfplan

###Specific resouce updated
#terraform plan -target=name.tf

###Apply environment variable name
#Set-item -Path env:TF_VAR_iam_user_name_prefix -Value 'FROM_ENV_VARIABLE_IAM_PREFIX'

###Get vars from certain file
#terraform apply -var-file="some-name.tfvars"

###Get subnets from aws to project
#terraform apply --target data.aws_subnets.default_subnets

###Select workspace
#terraform workspace select {name workspace}

###Show workspace
#terraform workspace show
